name: B0_PP_OCR_MOBILE_RAW_RULE_SMOKE
runtime:
  device: cpu
  threads: 4
ocr:
  plugin: mock
  params: {}
preprocess:
  plugin: none
  params: {}
selector:
  plugin: keyword_regex
  params:
    keyword_weight: 0.7
    negative_keyword_weight: 0.45
    confidence_weight: 0.2
    detection_confidence_weight: 0.2
    calendar_weight: 0.5
    pattern_weight: 0.25
    position_weight: 0.25
normalizer:
  plugin: date_ko_v1
  params:
    year_missing_token: NoNE
    none_token: NONE
